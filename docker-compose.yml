version: '3.8'

services:
  currency-service:
    container_name: currency-service
    build:
      context: .
    # Zamiast ports: "80:8084" dajemy expose, żeby tylko Nginx widział ten port
    expose:
      - "8084"
    depends_on:
      - kafka
      - postgres-db
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres-db:5432/analyzer_db
      SPRING_DATASOURCE_USERNAME: admin
      SPRING_DATASOURCE_PASSWORD: secret_pass
      SPRING_JPA_HIBERNATE_DDL_AUTO: update
      SPRING_KAFKA_BOOTSTRAP_SERVERS: kafka:9092
      # 👇 Zmienne dla Nginx Proxy, żeby automatycznie wygenerował SSL:
      VIRTUAL_HOST: currency-analyzer.duckdns.org
      VIRTUAL_PORT: 8084
      LETSENCRYPT_HOST: currency-analyzer.duckdns.org
      LETSENCRYPT_EMAIL: twoj-email@gmail.com # Wpisz swój adres email

  kafka:
    container_name: kafka-local
    image: apache/kafka:latest
    ports:
      - "9092:9092"
    environment:
      KAFKA_NODE_ID: 1
      KAFKA_PROCESS_ROLES: broker,controller
      KAFKA_LISTENERS: PLAINTEXT://0.0.0.0:9092,CONTROLLER://0.0.0.0:9093
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://kafka:9092
      KAFKA_CONTROLLER_LISTENER_NAMES: CONTROLLER
      KAFKA_CONTROLLER_QUORUM_VOTERS: 1@127.0.0.1:9093
      KAFKA_CLUSTER_ID: 4L62YDkVgSBlwGWw4Emsiw
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
      KAFKA_TRANSACTION_STATE_LOG_MIN_ISR: 1
      KAFKA_TRANSACTION_STATE_LOG_REPLICATION_FACTOR: 1
      KAFKA_HEAP_OPTS: -Xmx192M -Xms192M
      KAFKA_LOG_CLEANER_ENABLE: "false"

  postgres-db:
    container_name: postgres-db
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: analyzer_db
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: secret_pass
    volumes:
      - pgdata:/var/lib/postgresql/data:rw

  # 👇 DOPISUJEMY AUTOMATYCZNE PROXY (To co Docker usunął)
  nginx-proxy:
    image: nginxproxy/nginx-proxy:latest
    container_name: nginx-proxy
    ports:
      - "80:80"
      - "444:443" # Mapujemy na wolny port, jeśli 443 jest zajęty, lub klasycznie "443:443"
    volumes:
      - /var/run/docker.sock:/tmp/docker.sock:ro
      - certs:/etc/nginx/certs:ro
      - vhost:/etc/nginx/vhost.d
      - html:/usr/share/nginx/html
    restart: always

  # 👇 DOPISUJEMY COMPANION DO CERTYFIKATÓW SSL (Let's Encrypt)
  nginx-proxy-acme:
    image: nginxproxy/acme-companion:latest
    container_name: nginx-proxy-acme
    depends_on:
      - nginx-proxy
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - certs:/etc/nginx/certs:rw
      - vhost:/etc/nginx/vhost.d:rw
      - html:/usr/share/nginx/html:rw
    restart: always

volumes:
  pgdata: {}
  certs: {}
  vhost: {}
  html: {}